# Every execution ever, self-describing — reproducible from the pipeline, never committed.
runs/

# Local secrets and machine-specific env.
.env

# Uncommitted local factory state (workspace UUID for host-unit labels, etc.).
.cairn/
